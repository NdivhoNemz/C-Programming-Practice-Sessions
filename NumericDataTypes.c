#include <stdio.h>

int main()
{
    int dogs = 6; //for whole numbers or things that cant be divided

    /** 
     * doube and float are similar
     * if unsure, use double
     * difference have to do with precison
     * precision - how much data a variable can hold
     * double has double the preciosion
     * double takes double the memory, use float if memeory limited
     */

    printf("%i %f %f\n", 1, 1111.1111, 1111.1111F); //if you parse a float, it becomes a double

    return (0);
}
