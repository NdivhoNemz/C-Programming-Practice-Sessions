#include <stdio.h>

int main()
{
    int x; //variable declaration
    x = 10; // variable initialisation
    
    int y = x / 2;
    printf("If x is %i, then y is %i!\n", x, y);
    return (0);
}

//C is a space instensitive language.
//Variables = stores some date used in the program.
//Syntax = format of programming.
//Expression = Anything that can be evaluated to 1 value.
